/* TEMS Design System — modern, accessible, and scoped */
:root {
    --tems-font-sans: ui-sans-serif, system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial, "Apple Color Emoji", "Segoe UI Emoji";
    --tems-color-bg: #0b0c0f;
    --tems-color-surface: #0f1115;
    --tems-color-card: #151922;
    --tems-color-border: #222837;
    --tems-color-muted: #94a3b8;
    --tems-color-text: #e6edf3;
    --tems-color-accent: #3b82f6;
    /* blue-500 */
    --tems-color-accent-2: #22c55e;
    /* green-500 */
    --tems-shadow-sm: 0 1px 2px rgba(0, 0, 0, .15);
    --tems-shadow-md: 0 8px 24px rgba(0, 0, 0, .25);
    --tems-radius: 12px;
}

.theme-light:root {
    --tems-color-bg: #f8fafc;
    --tems-color-surface: #ffffff;
    --tems-color-card: #ffffff;
    --tems-color-border: #e5e7eb;
    --tems-color-muted: #475569;
    --tems-color-text: #0f172a;
}

/* Scope to TEMS containers to avoid affecting core */
.tems-branding,
.page-wrapper .tems-dashboard {
    font-family: var(--tems-font-sans);
    color: var(--tems-color-text);
}

.tems-dashboard {
    background: linear-gradient(180deg, rgba(59, 130, 246, 0.06), transparent 35%), var(--tems-color-bg);
    padding: 1.25rem 1.25rem 2rem;
    border-radius: var(--tems-radius);
}

.tems-dashboard .page-title {
    font-size: 1.375rem;
    font-weight: 700;
    letter-spacing: .2px;
    margin: 0 0 1rem 0;
}

.kpi-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(220px, 1fr));
    gap: 1rem;
}

.kpi-card {
    background: var(--tems-color-card);
    border: 1px solid var(--tems-color-border);
    border-radius: var(--tems-radius);
    padding: 1rem 1rem;
    box-shadow: var(--tems-shadow-sm);
    transition: transform .2s ease, box-shadow .2s ease, border-color .2s ease;
}

.kpi-card:hover {
    transform: translateY(-2px);
    box-shadow: var(--tems-shadow-md);
    border-color: color-mix(in oklab, var(--tems-color-accent) 30%, var(--tems-color-border));
}

.kpi-card .title {
    font-size: 0.9rem;
    color: var(--tems-color-muted);
    margin-bottom: .375rem;
}

.kpi-card .value {
    font-size: 1.75rem;
    font-weight: 800;
    letter-spacing: .3px;
}

.tems-pill {
    display: inline-flex;
    align-items: center;
    gap: .4rem;
    padding: .25rem .6rem;
    border-radius: 999px;
    background: color-mix(in oklab, var(--tems-color-accent) 14%, transparent);
    color: var(--tems-color-text);
    border: 1px solid color-mix(in oklab, var(--tems-color-accent) 24%, var(--tems-color-border));
}

.tems-muted {
    color: var(--tems-color-muted);
}

/* Compact cards for list & workspace blocks */
.frappe-card.tems-card,
.workspace .card.tems-card {
    border-radius: var(--tems-radius);
    border-color: var(--tems-color-border);
}

/* Head & navbar tweaks when wrapped in tems-branding */
.tems-branding .page-head,
.tems-branding .navbar {
    color: var(--tems-color-text);
}

/* Accessibility: focus rings */
.tems-branding a:focus,
.tems-branding button:focus,
.kpi-card:focus {
    outline: 2px solid color-mix(in oklab, var(--tems-color-accent) 40%, #fff);
    outline-offset: 2px;
    border-radius: var(--tems-radius);
}